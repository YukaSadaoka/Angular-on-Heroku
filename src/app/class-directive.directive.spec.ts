import { ClassDirectiveDirective } from './class-directive.directive';

describe('ClassDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = ClassDirectiveDirective;
    expect(directive).toBeTruthy();
  });
});
